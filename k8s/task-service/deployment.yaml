apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-service
  namespace: chronoflow
  # secretKeyRef Secrets (database, pub-sub, redis-auth) are synced from GCP SM by ExternalSecrets in k8s/external-secrets/.
  labels:
    app: task-service
    version: v1
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: task-service
  template:
    metadata:
      labels:
        app: task-service
        version: v1
    spec:
      containers:
        - name: task-service
          image: asia-southeast1-docker.pkg.dev/chronoflow-1/task-service/task-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: JAVA_OPTS
              value: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -Djava.security.egd=file:/dev/./urandom"
            - name: SERVER_PORT
              value: "8083"
            - name: NACOS_HOST
              value: "nacos.chronoflow.svc.cluster.local"
            - name: NACOS_PORT
              value: "8848"
            - name: NACOS_USERNAME
              value: "nacos"
            - name: NACOS_PASSWORD
              value: "nacos"
            - name: PROD_MASTER_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-master-db-host
            - name: PROD_MASTER_DB_PORT
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-master-db-port
            - name: PROD_MASTER_DB_USER
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-master-db-user
            - name: PROD_MASTER_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-master-db-password
            - name: PROD_SLAVE_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-slave-db-host
            - name: PROD_SLAVE_DB_PORT
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-slave-db-port
            - name: PROD_SLAVE_DB_USER
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-slave-db-user
            - name: PROD_SLAVE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database
                  key: prod-slave-db-password
            - name: PROD_REDIS_HOST
              value: "10.122.0.3"
            - name: PROD_REDIS_PORT
              value: "6379"
            - name: PROD_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: password
            # Spring Boot binds this directly to spring.data.redis.password (high precedence vs Nacos YAML).
            - name: SPRING_DATA_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: password
            - name: PUB_SUB_SERVICE_ACCOUNT_JSON
              valueFrom:
                secretKeyRef:
                  name: pub-sub
                  key: pub-sub-service-account
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: noti-config
                  key: gcp.project.id
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "900Mi"
              cpu: "600m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 240
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 240
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 10"]
      restartPolicy: Always
      terminationGracePeriodSeconds: 30